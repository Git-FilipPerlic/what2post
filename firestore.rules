rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The app only ever reads the daily images; only the Cloud Function
    // (Admin SDK, bypasses rules) writes them.
    match /dailyImages/{document} {
      allow read: if true;
      allow write: if false;
    }
  }
}
