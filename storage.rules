rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Images are generated server-side (Admin SDK, bypasses rules) and
    // served via their public URL, so no client access is needed here.
    match /{allPaths=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
